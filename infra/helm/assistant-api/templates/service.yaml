apiVersion: v1
kind: Service
metadata:
  name: {{ include "assistant-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "assistant-api.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
